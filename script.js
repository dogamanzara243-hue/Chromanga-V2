const USER = "dogamanzara243-hue"; const REPO = "Chromanga-V2"; const BASE = `https://raw.githubusercontent.com/${USER}/${REPO}/main`; window.onload = () => { const p = new URLSearchParams(window.location.search); const m = p.get('manga'); const b = p.get('bolum'); m && b ? loadReader(m, b) : loadHome(); }; async function loadHome() { const list = document.getElementById('manga-list'); if(!list) return; try { const r = await fetch(`${BASE}/manga-listesi.json`); const mangalar = await r.json(); list.innerHTML = ""; mangalar.forEach(m => { list.innerHTML += `<div class="manga-card" onclick="location.href='reader.html?manga=${m.slug}&bolum=1'"><img src="${m.cover}"><h3>${m.title}</h3></div>`; }); } catch(e) { list.innerHTML = "<p>Mangalar yüklenemedi.</p>"; } } async function loadReader(s, b) { const container = document.getElementById('image-container'); document.getElementById('manga-title').innerText = `${s.toUpperCase()} - Bölüm ${b}`; try { const r = await fetch(`${BASE}/veriler/${s}/bolum-${b}.json`); const data = await r.json(); container.innerHTML = ""; data.images.forEach(url => { const img = document.createElement('img'); img.src = url; container.appendChild(img); }); } catch(e) { container.innerHTML = `<h3 style="text-align:center;margin-top:50px">Bölüm Bulunamadı!</h3>`; } }